# TrendPulse — Learnings Ledger

Append-only. Stage 7 of `trendpulse-executor` writes one dated block per run.
`trendpulse-distill-learnings` periodically promotes durable lessons into agent memory
and marks promoted blocks with `<!-- promoted: <names> (YYYY-MM-DD) -->`.

Block format:

```
## YYYY-MM-DD — TASK-NNN <title>
- **Lesson:** … **Why:** … **How to apply:** …
- **Decision:** … **Rationale:** …
- **Gotcha:** …
```

---

<!-- learnings start below -->

## 2026-06-08 — TASK-001 Dev + infra environment
- **Lesson:** В Docker `COPY --from=<image:${TAG}>` НЕ поддерживает интерполяцию переменных. **Why:** buildkit резолвит `--from` до раскрытия ARG в строке образа → ошибка «variable expansion is not supported for --from». **How to apply:** выноси версионируемый внешний образ в отдельный stage (`FROM ghcr.io/astral-sh/uv:${UV_VERSION} AS uv`) и `COPY --from=uv …`. ARG до первого FROM = глобальный scope.
- **Lesson:** `uv sync --frozen` ставит и `default-groups` → dev-инструменты протекают в runtime-образ. **Why:** `[tool.uv] default-groups=["dev"]` удобен для локального CI, но образ один на api/worker/beat/migration_runner и уходит в prod. **How to apply:** в Dockerfile всегда `uv sync --frozen --no-dev`; dev-тулинг — только на хосте через `make ci`/`uv run`. Проверка: `docker run --rm <img> python -c "import pytest"` → ModuleNotFoundError.
- **Lesson:** Не клади пароль БД ни в код, ни в committed `deploy.env` (даже dev-дефолт задаёт паттерн). **Why:** CONVENTIONS — секреты только в `sensitive.env`/vault. **How to apply:** `Settings` собирает `database_url` из `POSTGRES_*` частей; пароль приходит из `sensitive.env`; `all.yml`/`deploy.env` держат только host/port/db/user.
- **Lesson:** `depends_on` без `condition: service_healthy` = гонка холодного старта (nginx → 502, celery → crash-loop до restart). **Why:** дефолт `service_started` ждёт только запуск процесса, не готовность. **How to apply:** дай api `healthcheck` (stdlib `urllib` на `/health`, без лишних пакетов), nginx → `depends_on api: service_healthy`; redis(healthy) в depends_on api/worker/beat.
- **Decision:** `ansible-unpack` на task-001 — dependency-free stub-рендерер `key: value` YAML → env (deploy.env из `all.yml`, sensitive.env из `vault.yml`, chmod 600). **Rationale:** локальный dev без `ansible` binary; реальный `ansible-vault` + secret-scan — **task-012**.
- **Gotcha (важно для task-002):** worker/beat сидят только на `postgres_net`/`redis_net` (`internal: true`) → **нет egress в интернет**. Telethon (MTProto) и pull моделей sentence-transformers в коллекторе сломаются. **How to apply:** в task-002+ дать worker egress-способную сеть (отдельная не-internal `egress`) ИЛИ кэшировать модель на build-time. Не наследовать молча.
- **Gotcha:** host-порт 443 часто занят другим локальным проектом (OrbStack/другой nginx). nginx.conf слушает только 80 локально (443 ssl — prod, под серты). **How to apply:** публикуй только `${HTTP_PORT:-80}:80`; 443 включай на prod вместе с TLS-блоком и сертами (ops/).
- **Gotcha (git hygiene):** `git rm --cached <dir>` оставляет файлы на диске, НО последующий `git checkout` на коммит/ветку, где dir был ещё tracked-then-removed, **удалит файлы с диска**. Так чуть не потеряли `.claude/` (хуки+скиллы) после merge finalize-PR. **How to apply:** untrack + gitignore в один заход и не переключайся на старые ревизии, ожидая файлы на диске; при потере — `git checkout <bootstrap-sha> -- <dir>` + `git restore --staged <dir>` (вернуть на диск, оставить untracked). Теперь `.claude`/`_bmad` untracked везде → переключения веток безопасны.

## 2026-06-08 — TASK-002 Data model (SQLAlchemy 2.0 · pgvector · multi-tenancy)
- **Lesson:** integration-тесты к БД нельзя гнать с хоста против compose-postgres (нет host-порта — изоляция из task-001). **Why:** network-design публикует только nginx. **How to apply:** для реального прогона integration-suite поднимай эфемерный `pgvector/pgvector:pg16` с проброшенным host-портом и `POSTGRES_HOST/PORT` env-override; продакшн-путь миграции проверяй отдельно через `migration_runner` + `docker exec psql` на изолированном стеке. (Оба пути прогнаны в G2.)
- **Decision:** baseline-миграция переиспользует СУЩЕСТВУЮЩИЙ `backend/migrations/` (task-001), не создаёт параллельный `alembic/`. `alembic.ini script_location=migrations`. **Rationale:** один alembic-скаффолд; план task-002 ошибочно говорил `alembic/` — диск важнее плана.
- **Decision:** pgvector без py.typed → mypy `ignore_missing_imports` через `[[tool.mypy.overrides]]` scoped на `pgvector.*` (НЕ inline `# type: ignore`). **Rationale:** CONVENTIONS запрещает inline-ignore; config-override локализован.
- **Gotcha:** размерность вектора (`EMBEDDING_DIM=384`) дублируется литералом в миграции (Alembic-миграции самодостаточны, не импортируют живой app-код) — ок, но **task-007 (pipeline) обязан матчить 384**, иначе Postgres отвергнет вставку. **How to apply:** при смене размерности — новая миграция + проверка `clusters.embedding` dim == EMBEDDING_DIM.
- **Gotcha:** репо-тесты строят схему через `Base.metadata.create_all`, а не миграцией → model↔migration drift не ловится round-trip-тестами (только `test_migrations` проверяет наличие таблиц). **How to apply:** при росте схемы добавить parity-check (autogenerate diff == empty).

## 2026-06-08 — TASK-003 Auth (fastapi-users + Google OAuth, JWT + cookie)
- **Lesson (КРИТИЧНО для локали):** `Secure` cookie НЕ отправляется браузером/curl по http. Локально стек http-only (:80) → и auth-cookie (`CookieTransport.cookie_secure`), и OAuth CSRF-cookie (`get_oauth_router(csrf_token_cookie_secure=...)`) должны быть `False` локально, иначе login/OAuth молча ломаются (401 / OAUTH_INVALID_STATE). **How to apply:** настройка `auth_cookie_secure` (код-дефолт True для prod, env `AUTH_COOKIE_SECURE=false` локально). Prod (task-012 `group_vars/prod.yml`) обязан выставить true.
- **Lesson:** fastapi-users 15 кладёт CSRF-токен в OAuth `state` (double-submit: state-JWT + cookie сверяются на callback). Ручной state → `OAUTH_INVALID_STATE`. **How to apply:** в тестах прогоняй реальный `/auth/google/authorize` чтобы получить валидный state + CSRF-cookie (мокая только `GoogleOAuth2.get_access_token`/`get_id_email`).
- **Lesson:** `oauth_accounts.expires_at` — INTEGER (int4, max 2147483647); мок unix-ts должен влезать (используй ~2000000000, не 9999999999).
- **Decision:** сохранили **int user id** (не UUID fastapi-users по умолчанию) — `FastAPIUsers[User,int]` + `IntegerIDMixin`, чтобы не ломать FK из task-002. Один `User` (расширили существующий `SQLAlchemyBaseUserTable[int]`), не второй. Миграция аддитивная (0002, down_revision 0001).
- **Decision (user-directive):** плоская раскладка `backend/src/` без уровня пакета `trendpulse/` — импорты top-level (`from config import`, `from storage...`, `from api...`). hatchling `[tool.hatch.build.targets.wheel] sources=["src"] only-include=["src"]`; mypy `mypy_path="src"` + `explicit_package_bases=true`; compose-команды `uvicorn api.main:app` / `celery -A celery_app`; alembic.ini `prepend_sys_path=src`. **Why:** запрошено пользователем; переопределяет CONVENTIONS §Repo layout (src-layout с пакетом). **How to apply:** новые backend-модули кладём прямо в `backend/src/<module>`, без `trendpulse.`-префикса.
- **Gotcha (prod-hardening долг → task-012):** (1) `auth_cookie_secure=true` на prod; (2) валидатор min-length ≥32 на `jwt_secret`/`oauth_state_secret` (дев-плейсхолдеры слабые, InsecureKeyLengthWarning); (3) шифрование Google токенов в `oauth_accounts` at rest; (4) `associate_by_email=True` полагается на Google-verified email (для строгости gate на is_verified). Защищённый контракт = `Depends(current_user)` (active, НЕ verified) — downstream-роуты выбирают gating осознанно.

## 2026-06-08 — TASK-004 Watchlist CRUD API
- **Decision (user-directive):** «одна junction-строка = один watchlist» — single channel на watchlist, адрес по числовому `id`; несколько каналов = несколько watchlists. Переопределяет multi-channel `channels: list` из task-doc. Лимит плана = макс. число watchlists на юзера (Free=5); `min_channels` = scoring-параметр, не счётчик каналов. **How to apply:** downstream (task-005 читает каналы watchlist'ов, task-006/008 берут topic+threshold) ожидают per-row watchlist с одним каналом.
- **Lesson:** sync-репозитории task-002 в async FastAPI-приложении — делай **sync `def` route-handlers** (FastAPI гонит их в threadpool; async-зависимость `current_user` всё равно резолвится) + sync session-dependency над `get_session`. Не тащить async-репозитории. (Auth остаётся на async-движке fastapi-users — два движка на одном psycopg-DSN, см. [[trendpulse-build-and-infra-gotchas]].)
- **Lesson (BOLA/IDOR):** tenant-изоляция держится на том, что `UserScopedRepository` НЕ наследует глобальный `get_by_id(by id)` — все выборки требуют `user_id`. Чужой/несущ. id → 404 (не 403/200, без утечки существования); tenant из токена (`get_tenant_user_id(user)`), НЕ из тела/пути; Pydantic `extra="forbid"` (нет mass-assignment `id`/`user_id`).
- **Gotcha:** дубликат `(user_id, channel_id, topic)` (unique из task-002) лови как IntegrityError → rollback → доменная ошибка → **409**, не 500. Покрой тестом (review поймал отсутствие).
- **Gotcha (seam-долг):** лимит — read-then-write (TOCTOU, неатомарен) + `len(list())` для count → task-010 (атомарный счётчик/индекс, пагинация). `validate_ref` сейчас format-only; когда task-005 даст `collector.registry` — проверь его на SSRF/timeout (вызывается на attacker-controlled handle на границе).

## 2026-06-08 — TASK-005 Source abstraction + Telegram collector
- **Lesson (session-генерация):** коды входа исчерпываются (`SendCodeUnavailableError`) после нескольких попыток; нестабильный MTProto-хэндшейк (`invalid new nonce hash` / `Server closed connection`) мешает доставке. Надёжный путь — **QR-login** (`client.qr_login()`, `exportLoginToken` — другой механизм, не коды): `development/scripts/get-telegram-session.sh` (обёртка: читает creds из sensitive.env, `uv run --with qrcode`, рисует QR в терминале). Сканировать может только владелец телефона — это неустранимый human-step (как код). См. `development/scripts/README.md`.
- **Lesson (egress):** worker сидел только в `internal: true` сетях → нет интернета (предсказано в task-001). Добавил **не-internal сеть `egress` только для worker** (collector→MTProto); api/postgres/redis остаются изолированы. Топология: worker = postgres_net+redis_net+egress.
- **Lesson (Telethon lifecycle — HIGH, поймал review):** клиент `connect()` на acquire без `disconnect()` = утечка сокетов/сессий в долгоживущем worker (+ `coroutine ignored GeneratorExit` в тестах). **Всегда** давай пулу/коллектору `aclose()` (дисконнект всех) + async-context-manager; в тестах/worker оборачивай в `async with`.
- **Decision:** ADR-001 порт реализован с разумными отклонениями от иллюстративной модели: `author: str | None`, `since: datetime | None` (backfill), `fetched_at` опущен. telethon — lazy-import (base.py SDK-free), mypy-override scoped на telethon.
- **Decision (user):** `POOL_MIN=1` на старте (одна dev-сессия), цель 3–10 — вернуть когда пул заполнен. FLOOD_WAIT mid-iteration может пере-выдать уже отданные посты → поглощается dedup по `external_id`.
- **Gotcha (secret hygiene):** StringSession = доступ к аккаунту уровня пароля; держать ТОЛЬКО в sensitive.env (gitignored)/vault, никогда в логах/repr/git/чате. `api_hash`, засвеченный в чате, стоит ротировать на my.telegram.org.

## 2026-06-08 — TASK-006 Celery infra (queues, beat, locks)
- **Decision (ADR-002 §2 refinement):** динамические per-user очереди `batch:user_{id}` **операционно непотребляемы** статическим worker `-Q` (Celery не умеет wildcard-подписку) → задачи копятся неисполненными. Перешли на **единую очередь `batch`** + per-user Redis-lock как гарант `max_instances=1`. Worker: `-Q celery,batch,score:global`. **Why:** lock уже даёт сериализацию по юзеру; шаринг очереди + горизонтальное масштабирование воркеров. **How to apply:** не плодить динамические очереди на тенанта; изоляция — через lock, не через имя очереди.
- **Lesson (review поймал HIGH):** проверяй, что worker реально ПОТРЕБЛЯЕТ очереди, в которые beat/диспетчер шлёт (не только default). G2 должна гонять beat→dispatch→consume, а не только `.delay()` в default.
- **Lesson (MEDIUM):** не зови `get_settings()` на уровне модуля — Settings с required-секретами (jwt_secret и т.п.) кинет ValidationError при простом импорте без env. Резолюй конфиг **лениво** внутри функции (`ttl: int | None = None` → settings при None).
- **Gotcha:** owner-checked Redis-lock release — атомарный CAS через `Redis.transaction` (WATCH/MULTI), не Lua → работает и на fakeredis (нет lupa). Сравнение токена идёт в bytes (`decode_responses=False`).
- **Gotcha:** postgres-том `development_postgres_data` переживает `make down`/`up` → данные прошлых задач (напр. watchlists из task-004) видны; полезно для G2 (диспетчер нашёл реального активного юзера), но помни при «чистых» прогонах (`docker volume rm`).

## 2026-06-09 — TASK-007 Batch pipeline (dedup→normalize→embed→cluster)
- **Lesson (HIGH, review поймал):** общий образ собирается `uv sync --frozen --no-dev` → `ml`-группа (torch/sentence-transformers) НЕ попадает в образ → worker падал бы ImportError на embed. **FIX:** Dockerfile build-ARG `INSTALL_ML`; **отдельный ml-worker-образ** (`APP_IMAGE_ML`, worker.yml `INSTALL_ML=true`), api/beat остаются на лёгком `APP_IMAGE`. **How to apply:** тяжёлые ML-деп — только в worker-образ через build-arg, не в общий; верь, что G2 «через worker» гоняет именно нужный образ.
- **Lesson:** `pytest.importorskip(...)` на уровне МОДУЛЯ исполняется на стадии collection даже для deselected (`-m 'not integration'`) тестов → при установленной опц. группе импортирует тяжёлый пакет и загрязняет `sys.modules`, ломая lazy-import-юнит-тесты. **How to apply:** skip-guard + heavy import — ВНУТРЬ тестовой функции, не на уровне модуля.
- **Lesson:** Redis read-and-clear (`lrange`+`delete`) неатомарен → конкурентный `rpush` между ними теряется. Делай за одну MULTI/EXEC-транзакцию (`pipe.lrange; pipe.delete; pipe.execute()`).
- **Decision:** embed — lazy singleton (импорт sentence_transformers ВНУТРИ `_load_model`, не на уровне модуля), имя модели из settings, выход строго `EMBEDDING_DIM`(384) с fail-fast. Шаги pure/immutable (frozen dataclasses, новые списки), платформо-независимы (не импортируют collector.telegram).

## 2026-06-09 — TASK-008 Scorer (viral score + alert trigger)
- **Decision:** виральный score — чистая детерминированная функция (overview §4: velocity·0.4+engagement·0.35+cross_channel·0.25; velocity=log1p(Δchannels)/Δhours; веса/факторы 3,2 — именованные; guard на zero-div). Платформо-независима (no collector import) — AC2-тест сканирует импорты score+tasks.
- **Lesson (идемпотентность alert):** unique `alerts(user_id, cluster_id)` (миграция 0003) + вставка под `session.begin_nested()` (SAVEPOINT) с catch IntegrityError → дубль откатывает ТОЛЬКО savepoint, не всю транзакцию (предыдущий Score/работа сохраняются). Гонка тиков безопасна.
- **Gotcha (footgun → task-009/011):** scoring идёт **per-TOPIC, не per-cluster** (нет post↔cluster FK в схеме task-002) → несколько свежих кластеров одной темы получают одинаковый score, каждый >порога = свой alert. **task-009 (delivery) обязан rate-limit'ить**; точный per-cluster score требует FK post→cluster (future). `channel_avg` сейчас = среднее окна (не исторический baseline) → engagement не «спайк-vs-норма». `Score` пишется каждый тик без upsert → ретенция в task-011.

## 2026-06-09 — TASK-009 Alert delivery (Telegram Bot + webhook)
- **Lesson (КРИТИЧНО, SSRF DNS-rebinding TOCTOU — обе стадии review+security поймали как HIGH):** валидировать URL резолвом DNS, а потом слать `httpx.post(url)` (повторный независимый резолв при коннекте) = **байпас** через low-TTL DNS (public IP на валидации, private/`169.254.169.254` на коннекте). **FIX:** резолв ОДИН раз → валидация всех IP (is_global deny-list) → **пиннинг TCP-коннекта к провалидированному IP** (кастомный `httpx.HTTPTransport`), Host/SNI/cert по реальному hostname, `follow_redirects=False`. Регресс-тест с разными IP на validate vs connect. **How to apply:** для любого user-supplied URL — resolve+validate+connect атомарны (pin IP), не доверяй повторному резолву httpx.
- **Lesson (mypy scope):** `[tool.mypy] packages=[...]` — при добавлении нового пакета его НАДО туда внести, иначе strict его НЕ проверяет (scorer/alerts были не покрыты — «mypy green» врал). После добавления всплыли реальные ошибки (Task generic args, `apply_async(args=tuple)` не list). **How to apply:** новый top-level пакет → сразу в mypy.packages; проверяй число «checked N source files».
- **Gotcha:** секрет в `@dataclass` → авто-`__repr__` его светит. `field(repr=False)` для токенов. bot_token/webhook_url plaintext at-rest → шифрование в task-012 (как OAuth-токены).
- **Decision:** delivery-config (telegram_bot_token/chat_id/webhook_url/plan) живёт на User (аддитивная миграция 0004); plan default "free" — seam, реальный биллинг task-010. dispatch_alert на default-очереди (worker уже потребляет), enqueue из scorer только для новых alert'ов.

## 2026-06-09 — TASK-010 Billing (NOWPayments crypto + plan enforcement)
- **Lesson (HIGH, idempotency vs multi-IPN):** платёжные шлюзы (NOWPayments) шлют НЕСКОЛЬКО webhook'ов на ОДИН платёж с одним id (waiting→confirming→confirmed→finished). Ключ идемпотентности должен быть «уже АКТИВИРОВАН» (terminal status), НЕ «видели payment_id» — иначе промежуточный IPN глушит активирующий и план не активируется никогда. `processed_at`/terminal-флаг ставь ТОЛЬКО на активации. Тестируй ПОСЛЕДОВАТЕЛЬНОСТЬ статусов, не один finished.
- **Lesson (webhook HMAC):** верифицируй HMAC `hmac.compare_digest` (constant-time) ДО парсинга/доверия телу; невалид/отсутствие/пустой секрет → reject (unset secret → 503, не верифицирует произвольное). NOWPayments подписывает sorted-key JSON — для надёжности лучше верифицировать по RAW-байтам (re-canonicalize рискует разойтись: ensure_ascii/float repr) — нужен реальный IPN-сэмпл для byte-проверки (долг).
- **Decision:** единая точка enforcement `assert_within_limit(session, user, resource)` (ADR-003); план писать ТОЛЬКО верифицированным IPN (no client-trust); invoice↔user binding + amount/currency cross-check против replay/cross-account; expired sub → effective FREE. PLAN_LIMITS — именованная таблица (overview §6), не литералы.
- **Lesson (test isolation, растущий suite):** по мере накопления integration-тестов всплывают: (a) тесты, требующие Redis/брокер (celery, scorer-enqueue) — скип-гардь Redis или делай side-effect устойчивым; (b) `[tool.mypy] packages` НЕ автоматический — каждый новый пакет добавляй вручную (alerts/scorer были не проверены strict). Гоняй ПОЛНЫЙ `-m integration` в G2, не только тесты текущей задачи.

## 2026-06-09 — TASK-012 Ops/IaC (Terraform + Ansible + secrets)
- **Decision:** Ansible — единственный источник истины по env (ADR-005 §4): `make ansible-unpack` = `ansible-playbook unpack-env.yml` рендерит `deploy.env` из group_vars + расшифровывает `sensitive.env` из **ansible-vault ENCRYPTED** vault (`$ANSIBLE_VAULT` blob в git; vault-пароль `.vault-pass` gitignored, только путь в ansible.cfg). Заменил dev-stub-рендерер из task-001. **How to apply:** при добавлении нового env-ключа — в group_vars (non-secret) или vault (secret) + templates/*.j2; проверяй что `ansible-unpack` отдаёт superset прежних ключей (ничего не потеряно — compose env_file сломается).
- **Lesson (env как single source):** `deploy.env` был закоммичен в task-001 → gitignore его не игнорил (tracked). `git rm --cached deploy.env` — теперь генерится Ansible, не второй источник. **How to apply:** материализуемые файлы не должны быть tracked.
- **Lesson (prod deploy coherence — review HIGH):** ansible deploy-плейбук с `docker_compose_v2` бесполезен, если не доставить app-source+compose+**version.env** на хост. Добавь `git`-clone источника (pinned `app_version`) в pre_tasks + `env_files: [version.env]` в compose-up (интерполяция image-тегов). Проверяемо офлайн через ansible-lint + `--syntax-check` (живой деплой требует VPS).
- **Decision:** Terraform — provider digitalocean (VPS+firewall+DNS+Spaces); `validate` через `init -backend=false` (офлайн, backend-креды — init-time `-backend-config`); firewall = network-design (только 443/80-redirect/SSH-allowlist, default 127.0.0.1/32); secrets `sensitive=true`, нет хардкодов; tfstate/tfvars gitignored. terraform/ansible/ansible-lint ставятся через brew (не на дефолтном PATH bash-сессий — префиксь `/opt/homebrew/bin`).
- **prod-долг закрыт:** `group_vars/prod.yml auth_cookie_secure=true` (task-009 hardening).

## 2026-06-09 — TASK-011 Compliance & ops (retention, GDPR, rate-limit, observability)
- **Lesson (HIGH, rate-limit keying):** slowapi `key_func` бежит в middleware ДО резолва route-deps, поэтому `request.state.user_id` от auth-зависимости там пуст → per-user keying мёртв (всегда IP). **FIX:** key_func сам декодит JWT из auth-cookie/Bearer (HS256, aud, БЕЗ БД-проверки — только `sub`), кэширует на request.state. **How to apply:** для per-principal rate-limit резолвь принципала в самом key_func (lightweight decode), не полагайся на upstream-dep.
- **Lesson:** за nginx `request.client.host` = IP прокси → IP-keying коллапсирует в один бакет. uvicorn `--proxy-headers --forwarded-allow-ips=<hop>` (api только за nginx → доверять можно). slowapi `get_remote_address` НЕ доверяет сырой XFF (не spoofable), но без proxy-headers бесполезен.
- **Lesson:** GDPR-delete = один `DELETE FROM users WHERE id` + DB-level `ON DELETE CASCADE` (task-002) → нет orphan'ов; эндпоинт скоупит ТОЛЬКО `current_user.id` (нет IDOR). Retention = bulk `UPDATE ... SET text=NULL WHERE fetched_at<now-48h` (по server-set времени ингеста, не source-controlled posted_at). Лог-гигиена: forbidden-key denylist + middleware без тел + celery без args (лучше allowlist — отмечено).
- **Lesson (probe timeout):** `/ready` должен биндить таймаут (redis `socket_timeout`/`socket_connect_timeout`), иначе зависший (не отказавший) бэкенд вешает probe. `/health` (liveness) остаётся чистым 200, `/ready` (readiness) проверяет зависимости — разделять.

## 2026-06-09 — TASK-013 Frontend foundation (C1, первая UI-задача)
- **Lesson (КРИТИЧНО, скопированный реф-проект ≠ только бренд+типы):** при адаптации эталонного SPA под свой продукт чужой код прячется НЕ только в видимом UI/типах, а в **серверном SSR-слое** (`frontend/server/**`): свой config (env `API_URL`/`COOKIE_SECRET` через Zod-required), Bearer-auth (читает токен из cookie → `Authorization: Bearer`), refresh-плагин на несуществующий эндпоинт. do почистил браузерный `client.ts`, но контейнер падал на старте (ZodError) — verify (G2 `make up`) поймал то, что build/tsc/unit пропустили. **How to apply:** при портировании ищи ВСЕ слои с auth/config (browser client + SSR server + proxy-плагины); `grep` по `Bearer|refresh|API_URL|COOKIE_SECRET`; обязательно поднимай реальный стек (build green ≠ runtime working).
- **Decision (статика вместо SSR):** источник всех рантайм-проблем — лишний node-SSR-сервер реф-проекта. Решение — **отдавать статику**: frontend-контейнер = `nginx:alpine` + Vite `dist` (SPA-fallback `try_files $uri /index.html`), `root.tsx` `createRoot` вместо `hydrateRoot+RouterClient` (гидрация требовала `window.$_TSR`, ронялась в статике). Убрало Bearer/refresh/COOKIE_SECRET/API_URL разом и совпало с network-design «статика за edge». Клиентский guard (TanStack `beforeLoad` + 401-interceptor `window.location.assign`) полностью покрывает auth-gate без SSR. **How to apply:** для SPA-фундамента предпочитай статику за nginx; SSR вводи только если реально нужен (и тогда проксируй исходный `Cookie`-заголовок, НЕ Bearer — fastapi-users cookie transport).
- **Lesson (nginx `/api` strip + cookie):** edge nginx: `location /api/ { proxy_pass http://trendpulse_api/; }` — **trailing slash на proxy_pass стрипает `/api`** (api на корне видит `/auth`,`/watchlists`,`/openapi.json`); `location / { proxy_pass http://frontend_spa; }`. `add_header ... always` на server-уровне наследуется обоими location (если location не объявляет свой add_header). `proxy_pass` сохраняет Set-Cookie httpOnly/Secure/SameSite verbatim. Клиент: `baseURL:'/api'` + `withCredentials:true`.
- **Lesson (cookie-auth UI-контракт для C2-C5):** никаких Bearer/refresh/localStorage-токенов в браузере; httpOnly-cookie ставит backend, JS её не читает. 401-redirect должен валидировать `?redirect=` (open-redirect A01: `isSafeRedirect` — только internal-путь, не `//`/внешний origin) И на interceptor-сайте, И на consumption-сайте (sign-in). Типы UI — ТОЛЬКО из backend `/openapi.json` (`openapi-typescript`), не ручные.
- **Gotcha (Node вне PATH):** node — через nvm (`~/.nvm/versions/node/v22.15.1/bin`), НЕ в дефолтном PATH bash-сессий и НЕ в /opt/homebrew/bin. Префиксь PATH в каждой команде. `NODE_VERSION` добавлен в `development/version.env`. Тестовые артефакты (`playwright-report/`,`test-results/`) — gitignore, не трекать.
- **Долг (→ task-014/C2):** удалить мёртвый `frontend/server/**` + fastify-deps + SSR-скрипты (`dev`/`start`) целиком; `shared/lib/error-codes.ts` выровнять под реальные коды TrendPulse backend; `manualChunks` для 729kB-бандла.

## 2026-06-09 — TASK-014 Auth flow UI (C2, cookie-auth SPA)
- **Lesson (no user-enumeration в SPA — security MED):** generic axios-interceptor, прокидывающий backend-`detail` в UI, ломает анти-enumeration: fastapi-users отдаёт `LOGIN_BAD_CREDENTIALS`/`REGISTER_USER_ALREADY_EXISTS` текстом → register-форма раскрыла бы существование email. **FIX:** на login/register UI показывает СТАТИЧНОЕ generic-сообщение («Invalid email or password» / «Could not complete registration»), не пробрасывая raw detail; e2e на register-duplicate проверяет отсутствие утечки. **How to apply:** для auth-форм никогда не показывай raw backend-error; единообразное сообщение для «неверный пароль» И «нет такого email».
- **Lesson (типы UI — только из gen.types):** добавил backend-роут, но забыл регенерировать `gen.types.ts` → `current_user` затипизировали руками → контракт-дрейф (молча разойдётся при смене типа поля). **FIX:** после ЛЮБОЙ backend-API-добавки — `npm run gen:api` (против живого `/api/openapi.json`) и использовать `components['schemas'][...]`, не ручные интерфейсы. **How to apply:** backend-роут + UI-потребитель в одной задаче → регенерация типов обязательна в том же PR.
- **Lesson (один источник 401-redirect и auth-состояния):** и axios-interceptor (hard `window.location.assign`), и router-guard (`navigate`) редиректили на один 401 → гонка/перезагрузка; AuthProvider (axios) + useCurrentUser (react-query) дублировали `GET /users/me` на старте → 2 источника состояния. **FIX:** `SKIP_REDIRECT_ON_401`-флаг на `GET /users/me` (interceptor пропускает, редиректит ТОЛЬКО guard); `AuthSync` зеркалит react-query→Zustand (без прямого axios). **How to apply:** один механизм 401-redirect; один источник current_user (react-query кэш), остальное — производное.
- **Gotcha (тест-БД vs alembic на общем volume — бьёт verify всех C-задач):** `backend/tests/conftest.py` строит схему `create_all`/дропает в teardown, но НЕ чистит `alembic_version` → после integration-прогона `make up` пропускает миграции (migration_runner видит head, а таблиц нет) → 500. **Mitigation в verify:** перед stack-e2e чистый том (`docker compose ... down -v`). **Долг:** изолировать тест-БД или чистить `alembic_version` в teardown.
- **Decision (тонкий read-роут вместо get_users_router):** `GET /users/me` сделан собственным read-only роутом (`api/auth/me.py`, `UserMeResponse` whitelist id/email/plan/is_verified) вместо `fastapi_users.get_users_router` — чтобы НЕ вводить PATCH-мутации (вне scope C2) и не светить лишние поля. `response_model` гарантирует, что секреты User (hashed_password/oauth-токены/telegram_bot_token) не утекают. plan дефолт = `Plan.FREE` (именованная константа). Источник current_user для C3 (лимиты) и C5 (биллинг).

## 2026-06-09 — TASK-015 Watchlists UI (C3, первый CRUD-флоу SPA)
- **Decision (паттерн маппинга backend-ошибок в UX — переиспользуем в C4/C5):** единый helper `shared/lib/backend-error.ts`: 402→quota-апселл-баннер, 403→feature-gate, 422→field-error (парс Pydantic `detail[]` по `loc`-пути, устойчиво к массиву/строке/пустому), 404→not-found-экран, 409→dup-сообщение. Пользователю НИКОГДА не показывается raw JSON/стек. **How to apply:** все формы C4/C5 маппят backend-статусы через этот helper; новые коды добавляй туда, не inline.
- **Lesson (backend — источник истины по лимитам):** не дублируй числа лимитов плана (Free=5/Pro=100/…) как magic literals на фронте — реагируй на `402`/`403` от backend и показывай текущий `plan` из `useCurrentUser`; числа для копи — из overview §6/типов. e2e реального 402 достигается через API (создать watchlist'ы до лимита Free=5), а не моком.
- **Lesson (Playwright strict-mode локаторы):** `getByText(широкий regex)` матчит несколько узлов → strict-mode падает; `getByText` не видит value инпутов. **How to apply:** для значений инпутов — assert по `inputValue`/role+value; для повторяющихся сообщений — `role=alert/status` + `.first()` или `containsText`; заголовки — `getByRole('heading')`.
- **Gotcha (react-query enabled-guard):** страница деталей с `id` из URL (`/watchlists/:id`) при невалидном id (`NaN`) всё равно дёргала `GET /watchlists/NaN` → паразитный 404. **FIX:** `enabled: !Number.isNaN(id)` в useQuery (UI рендерит not-found из NaN-guard без запроса). **How to apply:** любой `useQuery` с id из параметра роута — гейтируй `enabled` валидностью id.
- **Долг:** роут `/billing` создаётся в C5 (017) — до него апселл-ссылка ведёт в NotFound (санкционировано). XSS-инвариант: topic/handle/error рендерим только как JSX-текст (auto-escape), без dangerouslySetInnerHTML.

## 2026-06-09 — TASK-016 Alerts UI (C4, тонкий GET /alerts + лента)
- **Decision (тонкий read-API поверх pipeline-таблицы):** `GET /alerts` (list+detail) — отдельный read-only роут `api/alerts/`, читает таблицу `alerts` (task-008/009), join Cluster для `topic` (на Alert его нет). НЕ трогает scorer/delivery (cross-module read). `AlertRead` с `extra=forbid` — отдаёт только {id,score,topic,first_seen,channels_count,delivery_status}, не светит delivery_attempts/cluster_id/user_id (anti-enumeration/leak).
- **Lesson (plan-gating ОБЯЗАН быть на backend):** окно истории (Free=нет/Pro=30д/Team=90д) применяется в сервисе из `PLAN_LIMITS[Resource.HISTORY]` (Free→пустой список+`history_unavailable=True`), НЕ только в UI — иначе Free-юзер вытащит историю прямым API-вызовом с большим limit. UI лишь показывает `history_unavailable`+апселл. **How to apply:** любое plan-ограничение глубины/доступа данных — серверный фильтр, фронт не источник истины.
- **Lesson (пагинация — clamp, не reject):** `limit` клампится в сервисе к `[1, MAX=100]` (не `le=` на Query → не 422). Защищает от memory-DoS (нельзя дампнуть всю таблицу), но UX мягкий. Тест ассертит clamp. **Gotcha:** если поставить `le=` на Query — сломаешь clamp-тест; выбирай ОДНУ стратегию.
- **Lesson (defense-in-depth tenant join):** при join Alert→Cluster добавляй `Cluster.user_id == user.id` ДОПОЛНИТЕЛЬНО к `Alert.user_id` — cluster_id не должен мочь выдать чужой topic даже при аномалии данных. Обе таблицы — UserOwnedBase.
- **Gotcha (запуск integration-тестов локально):** conftest строит `database_url` из `POSTGRES_HOST/PORT/USER/PASSWORD/DB` (НЕ `DATABASE_URL`!), дефолт host=`postgres` (docker-имя, с хоста не резолвится). **Recipe:** поднять ephemeral `docker run -d --name X -e POSTGRES_*=... -p 15432:5432 pgvector/pgvector:pg16`, затем `POSTGRES_HOST=localhost POSTGRES_PORT=15432 ... uv run pytest -m integration`. conftest сам делает `CREATE EXTENSION vector` + create_all.

## 2026-06-09 — TASK-017 Billing & Account UI (C5, финальная C-задача)
- **Lesson (delivery-secret masking write-only):** `telegram_bot_token` — GET отдаёт ТОЛЬКО маскированно (`***last4` в `DeliveryConfigRead`, `extra=forbid`); полный токен НИКОГДА не в схеме/ответе/бандле/localStorage/URL/логах. Фронт-инпут `type=password`, очищается после save, шлётся ТОЛЬКО при непустом вводе (никогда null → не очистится случайно). **How to apply:** любой пользовательский секрет (токен/ключ) — write-only API + masked read; UI write-mostly.
- **Lesson (SSRF на user-supplied webhook — переиспользуй, не пиши свою):** PATCH webhook_url валидируется серверным task-009 `validate_webhook_url` (resolve+validate-all-IPs); НО авторитетная защита — на SEND-time (`build_ssrf_safe_client`/`PinnedIPTransport` re-resolve+pin), PATCH-валидация = fail-fast UX-гейт. Клиентская валидация (startsWith https, RFC1918-префиксы) — НАИВНАЯ, только UX, НЕ контроль (промахивает decimal/octal/IPv6/DNS-rebind). **How to apply:** SSRF-контроль = серверный на момент запроса; клиент и PATCH-гейт — вторичны.
- **Lesson (plan-gating фичи — серверный 403, не UI):** webhook = Pro+ через `assert_within_limit(Resource.WEBHOOK_DELIVERY)` (ADR-003 единая точка) → `PlanLimitExceeded(403)`; Free-юзер не сохранит webhook прямым PATCH. UI-апселл — косметика. План server-authoritative (`effective_plan` = DB + живая Subscription с expiry), не client-spoofable.
- **Gotcha (null-to-clear в PATCH):** `if data.field is not None:` молча игнорит явный null → пользователь не может ОЧИСТИТЬ поле. **FIX:** `data.model_fields_set` — применять поле если оно присутствует в теле (явный null чистит, omitted не трогает). Работает только если клиент шлёт ключ ТОЛЬКО при изменении (умная форма: `field !== current ? (val||null)` ) и write-only поля шлёт лишь непустыми.
- **Gotcha (цена-константа дублируется и расходится):** Team price был $49 во фронте (и в unit-тесте!), а источник истины (overview §6 + backend `plans.py _TEAM_PRICE_USD=79`) = $79. Тест закодировал баг. **How to apply:** числа тарифов для копи держи в ОДНОМ модуле (`entities/plan/constants`), сверяй с backend `PLAN_PRICES_USD`; платёжная сумма — ВСЕГДА из `InvoiceResponse` (не inline). При расхождении кода и теста — источник истины overview/backend, чини оба.
- **Decision (no Stripe, крипто-инвойс):** апгрейд = `POST /billing/invoice` (NOWPayments, сумма считается backend), UI показывает адрес/сумму/статус из `InvoiceResponse`; план активирует ТОЛЬКО backend по IPN (UI IPN не дёргает). Реальный invoice e2e требует NOWPayments-ключ (вне dev) → e2e стабит `page.route`. `/billing` роут закрыл долг C3 (апселл-ссылка из watchlists).

## 2026-06-09 — TASK-018 Landing base (B1, последняя задача UI-пака)
- **Lesson (КРИТИЧНО, compliance-честность в legal-страницах template):** скопированный лендинг тащит legal-страницы (privacy/ToS/cookie/DPA/do-not-sell) с раскрытиями ЧУЖОГО продукта — review поймал 2 HIGH: (1) legal декларировали **Stripe как платёжный суб-процессор** + `__stripe_*` куки, хотя продукт «никакого Stripe, только крипта» (overview §6) → ложное/вводящее в заблуждение публичное раскрытие; (2) `/contact` делал **live `fetch('/api/waitlist')`** — нарушение «лендинг = статика, независим от backend» (битая форма + PII-сбор без приёмника). **How to apply:** при адаптации template-лендинга legal/compliance-страницы — НЕ только бренд: вычищай суб-процессоров/куки/обещания чужого продукта, сверяй с overview §6/§7; grep `stripe`/`fetch('/api`/чужой-бренд = 0 в src; маркетинговые «No Stripe» — ок. Бренд в site-config ≠ чистота legal (бренд был 0 хитов, а Stripe в legal остался).
- **Lesson (брендинг прячется в инфра-копи):** `pb_*` префикс куки в cookie-policy (PostBolt-остаток) — бренд утёк не как строка «PostBolt», а как префикс идентификаторов. Grep по бренду должен включать известные префиксы/коды чужого проекта. Статический лендинг не ставит session/csrf-куки — описывать их в cookie-policy нечестно.
- **Decision (CTA на SPA из конфига):** signupUrl в `public/config.json` (не inline), `/sign-up` относительный — предполагает edge-routing (один домен лендинг+SPA); при отдельном домене SPA → абсолютный URL. e2e проверяет href-подстроку.
- **Gotcha (Lighthouse офлайн):** Lighthouse требует Chromium remote-debug порт — недоступен в headless-sandbox. SEO-AC закрывать через `seo:validate` (project script) + curl SSR (title/meta/OG/canonical в HTML), Lighthouse-порог — в CI. Не блокировать ship на отсутствии Lighthouse-харнесса.
- **Pattern (review > security для compliance-честности):** security-стадия (OWASP-фокус) пропустила Stripe-в-legal (не секрет/не XSS), а **review** поймал как HIGH (противоречие источнику истины). Обе стадии нужны — review ловит фактологию/консистентность, security — уязвимости.

## 2026-06-09 — TASK-019 API DX (Epic D — Swagger gating + офлайн OpenAPI client-gen + удаление error-codes)
- **Decision (Swagger gating = fail-secure default + IaC override):** `Settings.swagger_enable: bool = False` (code-default закрыто), в `api/main.py` чистый helper `_docs_urls(flag)` → при off `docs_url=None,redoc_url=None,openapi_url=None` в конструктор `FastAPI(...)` (роуты НЕ регистрируются → реальный 404, не 403/пустая страница; гасит и Swagger-UI, и сам JSON). Включение — ТОЛЬКО явным env `SWAGGER_ENABLE` (dev). IaC: `group_vars/all.yml swagger_enable:"true"` (dev), `group_vars/prod.yml swagger_enable:"false"` (prod override), template `deploy.env.j2` рендерит. Защита в глубину: даже забытый override → код по умолчанию закрывает. **How to apply:** любой dev-only/опасный эндпоинт гейть так же — безопасный код-дефолт + IaC-оверрайд, не наоборот.
- **Gotcha (deploy.env gitignore'нут — правь ansible-источник, не рендер):** `development/env/deploy.env` рендерится `make ansible-unpack` из `group_vars` и НЕ коммитится — правка флага прямо в нём эфемерна (теряется при следующем unpack). Коммитимый источник non-secret env = `ops/ansible/roles/env/templates/deploy.env.j2` + `group_vars/{all,prod}.yml`. **How to apply:** добавляешь env-переменную для контейнеров — заводи её в template + group_vars (dev в all.yml, prod-оверрайд в prod.yml), а не в рендеренный файл.
- **Decision (офлайн OpenAPI-дамп вместо живого localhost):** `backend/scripts/dump_openapi.py` импортит `app`, `app.openapi_schema=None` (сброс кэша) → `json.dumps(app.openapi(), sort_keys=True, indent=2)+"\n"` (ДЕТЕРМИНИЗМ — иначе drift-check ложно падает) в коммитимый `frontend/src/shared/api/openapi.json`. Фронт `gen:api` берёт типы из этого файла, НЕ из `http://localhost/api/openapi.json` → воспроизводимо в CI без `make up`. `make gen-openapi` сервер не поднимает (синхронная сборка схемы из роутов, без сети/БД). Импорт app требует fail-fast секретов Settings → запуск с dummy `JWT_SECRET=dump` и т.п. (`GEN_DUMP_ENV` в Makefile). **How to apply:** контракт-артефакты генерь офлайн из app-объекта + коммить + drift-check, а не из поднятого стека.
- **Lesson (drift-check: `git status --porcelain`, НЕ `git diff --exit-code`):** review поймал HIGH — `git diff --exit-code` СЛЕП к untracked-файлам, поэтому пока сгенерённый `openapi.json` не закоммичен, дрейф самого дамп-файла молча проходит (защищался только tracked `gen.types.ts`). **FIX:** `openapi-drift-check` проверяет `[ -n "$(git status --porcelain -- $DUMP $TYPES)" ] && exit 1` — ловит и untracked (`??`), и modified (`M`). **How to apply:** проверка «сгенерённое закоммичено» по untracked-возможным артефактам — через `git status --porcelain`, не `git diff`.
- **Lesson (scripts/ вне дефолтного mypy):** mypy-конфиг проекта = `packages=[...]` под `src/` → `backend/scripts/` НЕ проверяется дефолтным `make ci`/`typecheck`. **FIX:** явный шаг `$(UV) mypy scripts/dump_openapi.py` в `ci` и `ci-fast`. **How to apply:** новый python-код вне `src/` (скрипты/утилиты) — добавляй отдельной mypy-целью, иначе strict в CI его не покрывает.
- **Decision (долг error-codes из TASK-013 закрыт удалением):** `frontend/src/shared/lib/error-codes.ts` (107 строк ЧУЖИХ PostBolt-кодов 10xxx–60xxx) удалён — TrendPulse backend такие коды не отдаёт, карта мёртвая и вводила в заблуждение. `resolveErrorMessage` теперь inline в `client.ts`: `(fallback?) => fallback ?? GENERIC_ERROR_MESSAGE` (берёт backend-`message`/Zod-issues либо generic-фразу, не пустую строку/undefined), поле `ErrorBody.code` убрано, реэкспорт из `shared/lib/index.ts` снят. e2e (28) подтвердил — клиентские флоу целы. **How to apply:** при адаптации template-кода чужие доменные карты ошибок — удаляй, не «выравнивай»; маппинг backend-ошибок в UX уже есть в `backend-error.ts` по HTTP-статусам.
- **Gotcha (локальный Docker-образ кэширует код — verify врёт):** новый `Settings.swagger_enable` не виден в контейнере без `make build` (`SettingsConfigDict(extra="ignore")` молча глотает неизвестный env, а старый образ поля не знает) → G2-проверка флага даст ложный результат на устаревшем образе. **How to apply:** перед G2-проверкой изменений в backend-коде/Settings локально — `make build`; CI пересобирает образ сам.
- **Non-blocking follow-up:** (1) `make ci` шаг `gen-types` требует node/`npm ci` в окружении — CI-джоба (TASK-021) должна ставить ОБА тулчейна (uv + frontend npm). (2) Security предложил ansible-assert `prod.yml.swagger_enable=="false"` — кандидат в TASK-032 (security hardening).

## 2026-06-09 — TASK-020 Alerts cursor-пагинация (Epic D — keyset + составной индекс)
- **Decision (keyset вместо offset для растущей ленты):** `GET /alerts` переведён с offset/limit на keyset по `(first_seen DESC, id DESC)`. Предикат `tuple_(Alert.first_seen, Alert.id) < tuple_(literal(fs), literal(id))` (row-value сравнение, Postgres-native), `ORDER BY first_seen DESC, id DESC LIMIT n+1` (fetch+1 определяет наличие следующей страницы). Tiebreaker `id` ОБЯЗАТЕЛЕН — `first_seen` не уникален, без него дубли/пропуски на границе. Составной индекс `ix_alerts_user_first_seen(user_id, first_seen)` (миграция 0006) покрывает и tenant-предикат, и сортировку → O(log N) вместо deep-offset скана. **How to apply:** любая растущая tenant-лента с сортировкой по времени → keyset, не offset; индекс `(tenant_col, sort_col)`; tiebreaker по PK обязателен если sort_col не уникален.
- **Decision (opaque cursor):** `next_cursor` = base64url(JSON `[first_seen.isoformat(), id]`), `_encode/_decode_cursor`. Клиент не строит SQL-фильтр — сервер декодит и подставляет через bind-params (`literal()` → BindParameter, НЕ f-string). Cursor несёт ТОЛЬКО позицию, не права: `user.id` — из auth-сессии, фильтры `user_id`/`cutoff` безусловны и применяются ДО keyset (AND-семантика) → подделкой cursor нельзя выйти за tenant/history-окно. **How to apply:** opaque-cursor кодирует позицию, никогда не identity/scope; фильтры доступа всегда независимы от cursor.
- **Lesson (битый cursor НИКОГДА не 500 — валидируй ВЕСЬ декод):** недоверенный cursor (base64 от клиента) → `_decode_cursor` в `try/except → InvalidCursorError` → router маппит в **422**. Security поймал: одного try мало — гигантский int в JSON (`2**63`) проходит type-check `isinstance(int)`, но в БД даёт int8-overflow `DataError` → НЕобёрнутый 500 (нарушение инварианта). **FIX:** валидируй диапазон (`_MIN/_MAX_CURSOR_ID` = 0..2**63-1) И отклоняй `bool` (он подкласс int → `[iso, true]` иначе = id=1) ВНУТРИ декодера, до SQL. Тест `test_out_of_range_cursor_id_not_500`. **How to apply:** при парсинге недоверенного ввода в значение для БД — валидируй диапазон/тип под целевую колонку, не только Python-тип; 500 на crafted-вводе = дыра.
- **Lesson (keyset round-trip на полной точности timestamp):** review поймал — тест равных `first_seen` использовал `microsecond=0`, не покрывая реальный риск: рассинхрон микросекундной/tz-репрезентации между encode (из ORM-объекта) и WHERE-bind на граничном значении. **FIX:** tiebreaker-тест на `microsecond=123456` — ловит truncation/tz-drift. `_encode_cursor` нормализует к UTC (`astimezone(UTC)`), `first_seen` колонка — `DateTime(timezone=True)`. **How to apply:** тесты keyset-границы — с ненулевыми микросекундами и tz-aware, иначе precision-баг не виден.
- **Gotcha (unit-тест, дублирующий логику = ложное покрытие):** фронтовый unit копировал `getNextPageParam` локально («mirror of queries.ts») вместо импорта — регрессия в queries.ts (null вместо undefined) осталась бы зелёной. **FIX:** вынес `alertsNextPageParam`/`ALERTS_INITIAL_PAGE_PARAM` экспортами из `queries.ts`, unit импортирует реальные. **How to apply:** инлайн-логику в конфиге хука (useInfiniteQuery getNextPageParam) выноси в экспортируемый хелпер, чтобы тест проверял прод-код, а не копию.
- **Decision (контракт-обмен offset→cursor разом):** `AlertListResponse{items,next_cursor:str|None,history_unavailable}` (убраны total/limit/offset); фронт — единственный потребитель, меняли оба разом. `useAlerts` infinite по `next_cursor` (initialPageParam null, getNextPageParam → next_cursor ?? undefined, стоп на null). `list.tsx` не тронут (абстракция hasNextPage/fetchNextPage/page.items). Типы офлайн-регенерены из дампа TASK-019 (`make gen-openapi gen-types`), не из живого стека. tenant-scope + history-window из TASK-016 сохранены без изменений.
- **Gotcha (pre-existing flaky e2e):** `frontend/tests/e2e/watchlists.spec.ts:36 (AC1 create watchlist)` фликует таймаутом (`@testchannel` не появляется за 5s) — НЕ связано с диффами alerts; alerts.spec.ts изолированно 4/4. Не блокер для не-watchlist задач; кандидат на стабилизацию в watchlist-задаче (увеличить ожидание/детерминировать сид).

## 2026-06-09 — TASK-021 CI foundation (Epic D — корневые workflows + conftest-изоляция + coverage + dep-scan)
- **Lesson (GitHub Actions запускает workflow ТОЛЬКО из `<git-root>/.github/workflows/`):** реальные workflow лежали в `frontend/.github/workflows/` и `landing/.github/workflows/` (PostBolt-наследие, бренд `postbridge01`) → GitHub их НЕ исполнял, CI де-факто не было. git-root тут = `apps/trendPulse` (`git rev-parse --show-toplevel`). **FIX:** корневые `.github/workflows/{pr-checks,main-integration}.yml`; вложенные удалены. **How to apply:** workflow только в корне репо; вложенные в подпапки молча игнорируются — не дублировать; проверяй git-root, а не cwd.
- **Lesson (CRITICAL для общего postgres-volume — conftest должен чистить `alembic_version`):** `conftest db_engine` делал `Base.metadata.drop_all/create_all`, но `alembic_version` вне `Base.metadata` (её ставит migration_runner) → после integration на общем dev-volume оставалась stale-версия (напр. 0006) при дропнутых таблицах → следующий `make up` думал «уже на head», пропускал миграции → битая схема/500. **FIX:** в teardown `DROP TABLE IF EXISTS alembic_version` (идемпотентно). Доказано на ephemeral PG: после сьюита to_regclass(alembic_version)=NULL, повторный upgrade чист. **How to apply:** тесты, строящие схему через `create_all` мимо alembic, ОБЯЗАНЫ сбрасывать `alembic_version` в teardown, иначе отравляют миграционный стейт общего тома.
- **Decision (PR быстрый без сервисов; integration/e2e — только push main):** `pr-checks.yml` (pull_request) = lint/type/unit+coverage/dep-scan/openapi-drift — без БД/стека. `main-integration.yml` (push main) = integration (postgres/redis GitHub services) + e2e (`make up` полный стек + Playwright за nginx). **How to apply:** дорогие стек-зависимые проверки не вешай на каждый PR — гоняй на merge в main; PR держи быстрым.
- **Decision (coverage-gate в `make test-cov`, НЕ в global addopts):** замерил unit-only = 81.82% → `[tool.coverage.report] fail_under=80` (порог именован в pyproject, не magic в CLI), отдельная цель `make test-cov` (`pytest -m 'not integration' --cov`). `--cov` НЕ в `addopts` → `ci-fast`/обычный прогон остаются быстрыми (coverage не считается каждый раз). **How to apply:** перед включением gate ЗАМЕРЬ покрытие; gate ставь в отдельную цель, не в global addopts; порог именуй в конфиге.
- **Lesson (pre-commit не должен требовать БД):** local hook звал `make ci` (полный → `pytest` с integration) на pre-push → виснет без postgres. **FIX:** `entry: make ci-fast` (без integration); полный `make ci` остаётся для main-workflow/ручного. **How to apply:** pre-commit/pre-push гоняет только то, что работает без внешних сервисов.
- **Lesson (dep-scan: ignore-by-id + hard-fail, НЕ сплошной continue-on-error):** review поймал — backend pip-audit под полным `continue-on-error` пропускал бы и НОВЫЕ CVE молча. **FIX:** `pip-audit --ignore-vuln <id>` для 7 известных upstream-CVE (pillow×6+pytest) по id (видимость в YAML) + hard-fail на остальном (проверено exit 0). frontend (0 CVE) — hard-fail. landing (12 транзитивных high/critical, нужен --force) — оставлен continue-on-error+TODO. **How to apply:** известные неустранимые CVE глуши поимённо `--ignore-vuln`, а не отключай весь gate — иначе регресс новых уязвимостей невидим.
- **Gotcha (`make logs -f` в CI виснет на провале):** e2e на провале health звал `make logs` (=`compose logs -f`, follow) → шаг висит до global timeout (~6ч). **FIX:** нефолловая цель `make logs-once` (`logs --no-color --tail=500`). **How to apply:** в CI никогда не используй follow-логи (`-f`/`tail -f`) на диагностическом пути — только bounded-дамп.
- **Gotcha (GitHub Actions script-injection / триггеры):** `${{ github.head_ref }}` (недоверенный) безопасен в `concurrency.group` (не shell), но в `run:` = RCE-вектор. Заменил на `github.ref` для гигиены. Триггеры строго `pull_request` (форк без секретов) и `push main` (доверенный merge) — НИКАКОГО `pull_request_target`/`workflow_run` с checkout+секретами. `permissions: contents: read` top-level (least privilege). CI-секреты — только dummy для ephemeral-стека (heredoc `<<'SENSITIVE'` не раскрывает переменные). **How to apply:** не интерполируй `github.event.*` в `run:`; избегай `pull_request_target`; ставь минимальные `permissions`; реальные секреты — через `secrets.*`, не хардкод.
- **Gotcha (недетерминированные build-артефакты = scope-drift):** `landing npm run build` зовёт `sitemap:gen`, переписывающий `sitemap.xml` с текущим timestamp → дрейф в диффе при каждом прогоне CI-проверки локально. Откатил. **How to apply:** прогон build в verify может насорить детерминированно-нестабильными артефактами — проверяй `git status` и откатывай вне-scope дрейф перед ship; кандидат — зафиксировать `lastmod` на дату контента.

## 2026-06-09 — TASK-022 Scoring correctness (Epic D — per-cluster scoring + posts.cluster_id FK)
- **Lesson (P0 — `Post`-строки вообще не персистились в проде):** пайплайн (`batch_processor.process_user_batch`) писал ТОЛЬКО `Cluster`; посты жили как in-memory `NormalizedPost` в `ClusterCandidate.posts` и сидились лишь в тестах вручную. → scorer в проде читал пустую `posts` → реальных скоров/алертов не было. Это корень «scoring correctness», а не просто per-topic→per-cluster рефактор. **FIX:** batch персистит `Post` с `cluster_id` при персисте каждого кластера (резолв channel_id по (source_kind, handle) одним запросом, без N+1; нерезолвнутый канал → warn+skip). **How to apply:** проверяй, что данные, которые читает downstream (scorer), реально ПИШУТСЯ upstream (pipeline) — отсутствие персиста легко не заметить, тесты сидят данные сами.
- **Decision (per-cluster engagement через FK):** `posts.cluster_id → clusters.id` (nullable, ON DELETE SET NULL — посты переживают удаление кластера). `_build_score_inputs` агрегирует по `Post.cluster_id == cluster.id` (tenant-scoped user_id), а `watched_channels_count` (cross_channel-знаменатель) — из watchlist-config темы. Окна `posted_at` на постах кластера НЕ нужно — `_recent_clusters` уже гейтит свежесть по `Cluster.updated_at`. Раньше per-topic агрегат (по всем каналам темы) давал ОДИН score-вход для всех кластеров темы → визуальные дубли алертов. **How to apply:** engagement/score считай по сущности кластера, а не по теме; FK-линк обязателен.
- **Decision (Score upsert вместо insert-каждый-тик):** `uq_scores_user_cluster (user_id, cluster_id)` + `_persist_score` через `pg_insert(...).on_conflict_do_update(constraint="uq_scores_user_cluster", set_={...computed_at...})` → одна строка на пару, не растёт линейно с тиками. Миграция дедупит существующие (`DELETE FROM scores a USING scores b WHERE a.id<b.id AND ...`) ПЕРЕД созданием unique (иначе upgrade падает на дублях). **How to apply:** периодический пересчёт метрик → upsert по натуральному ключу, не append; при вводе unique на живой таблице — сначала дедуп в той же миграции.
- **Gotcha (тестовое покрытие новой write-логики — CRITICAL на review):** unit-тест batch патчил `_build_handle_to_channel_id`→{} → ветка `session.add(Post(...))` была мёртвой зоной; integration `test_run_batch` скипается без `sentence_transformers` (ML opt-in) и Post не ассертил. Центральный AC (персист Post с cluster_id) НЕ проверялся ни одним исполняемым тестом — регрессия маппинга/FK прошла бы зелёной. **FIX:** детерминированный unit с фейк-энкодером (`embed._get_model` mock) + непустой channel-картой + `_install_id_assigning_flush` (mock flush присваивает Cluster.id как Postgres autoincrement) → ассерт Post.cluster_id/channel_id/метрики. **How to apply:** новую write-ветку покрывай детерминированным тестом БЕЗ тяжёлых зависимостей (мокай ML-энкодер фейком); патч-в-{} даёт ложное «зелёное».
- **Gotcha (mock session.flush должен присваивать PK):** код `add(Cluster)→flush()→add(Post(cluster_id=cluster.id))` зависит от того, что flush populates `cluster.id`. Наивный mock flush (просто счётчик) оставляет id=None → Post.cluster_id=None молча. Фейк-flush должен присваивать инкрементный id добавленным Cluster без id (зеркалит БД). **How to apply:** при unit-тесте flush-then-read-PK паттерна — mock flush обязан симулировать присвоение PK.

## 2026-06-09 — TASK-023 Reliability (Epic D — pending-sweep + Celery /ready + alerts-by-status)
- **Gotcha (CRITICAL — circular import от scheduler→package.constants через ТЯЖЁЛЫЙ __init__):** добавил `RESWEEP_PENDING_ALERTS_TASK` в leaf `alerts/constants.py` (как `pipeline.constants`/`compliance.constants`), но worker крашился: `celery_app → scheduler → from alerts.constants import ... → запускает alerts/__init__.py → реэкспортит alerts.tasks → import celery_app (mid-init) → ImportError`. Корень: `alerts/__init__` был ТЯЖЁЛЫМ (реэкспортил celery_app-зависимый `alerts.tasks`), в отличие от ЛЁГКИХ `pipeline/__init__`/`compliance/__init__` (которые НЕ реэкспортят свои `.tasks`). До TASK-023 цикла не было, т.к. ничто в цепочке celery_app→scheduler не импортило пакет `alerts`. **FIX:** сделать `alerts/__init__` лёгким — убрать `from alerts.tasks import ...` (никто не использовал package-level dispatch_alert; scorer импортит `from alerts.tasks import dispatch_alert` ЛЕНИВО внутри функции). **How to apply:** `<pkg>/constants.py` спасает от цикла ТОЛЬКО если `<pkg>/__init__.py` сам лёгкий (не тянет celery_app); импорт любого подмодуля запускает __init__ пакета. НЕ ловится unit-тестами (изолируют импорты) — ловится реальным стартом worker в Docker (G2). Держи доменные `__init__` лёгкими, Celery-seam импортируй явным подмодулем.
- **Decision (re-sweep зависших pending):** beat-задача `resweep_pending_alerts` (интервал/grace из Settings) — `SELECT id WHERE delivery_status='pending' AND first_seen < now()-grace ORDER BY first_seen LIMIT max_batch` → `dispatch_alert.apply_async(id)`. Идемпотентно: только `pending` (не failed=терминал/не delivered), grace отсекает in-flight, `notifier.deliver` гардит DELIVERED. Закрывает footgun task-008 (потерянный enqueue при падении брокера/worker → alert навсегда pending). Индекс `ix_alerts_status_first_seen(delivery_status, first_seen)` под системный sweep-скан (миграция 0008). **How to apply:** для «потерянной» async-работы — периодический idempotent re-sweep по статусу+возрасту с grace и batch-cap, а не надежда на единственный enqueue.
- **Lesson (re-enqueue в sweep должен пережить недоступный брокер):** review поймал — `apply_async` в цикле без try/except: если брокер лежит (ровно тот сценарий, ради которого sweep есть), исключение прервёт цикл и пропустит метрику. **FIX:** try/except+warning+continue (как scorer `_enqueue_delivery`), `emit_alerts_by_status` отрабатывает всегда (backlog-сигнал важнее всего именно при сбое). **How to apply:** enqueue в recovery-задаче оборачивай try/except — recovery-код обязан быть устойчив к той же поломке, которую чинит.
- **Decision (/ready проверяет Celery worker, bounded):** `_check_celery` = `inspect(timeout=celery_ping_timeout_seconds).ping()` (непустой ответ=жив), try/except→False, как `_check_db`/`_check_redis`; `/ready` 503 при db|redis|celery down, `/health` остаётся чистой liveness. Доставка зависит от worker → readiness должен его учитывать. **Tradeoff (follow-up):** control-bus ping может дать ложный 503 при перегрузке у живого worker (flapping при масштабировании) — кандидат на Redis-heartbeat-ключ вместо синхронного ping. **How to apply:** readiness учитывает зависимости, от которых зависит обслуживание; но синхронный control-bus ping — не лучший liveness-сигнал под нагрузкой, heartbeat надёжнее.
- **Decision (alerts-by-status через log_event):** `emit_alerts_by_status` = `GROUP BY delivery_status COUNT`, zero-fill, эмит через `observability.log_event` (aggregate-only — counts допустимы, raw-content структурно запрещён). Источник алертинга на рост pending (застой доставки). **How to apply:** метрики-счётчики — через aggregate-only структурный логгер, не печатай сырьё.

## 2026-06-09 — TASK-024 Observability: Sentry + correlation/trace-id (Epic D)
- **Lesson (CRITICAL security — Sentry утекает секреты через stacktrace frame locals):** `sentry_sdk` по умолчанию `include_local_variables=True` → в каждый фрейм стектрейса пишется `repr()` локальных переменных. `before_send`-scrub чистит только `request`/`extra`/`contexts`, но НЕ `exception.values[].stacktrace.frames[].vars`. В проекте реальные секреты в локалах: Telegram `session`(StringSession=authkey)/`api_hash`, `password`, `Settings`-креды (postgres_password, nowpayments_api_key). При любом unhandled-исключении они ушли бы в Sentry в обход scrub. **FIX:** `include_local_variables=False` в `sentry_sdk.init` (repr-строки локалов надёжно не отскрабить — единственно верно их не собирать). **How to apply:** при подключении любого error-трекера с capture локальных переменных — ОТКЛЮЧАЙ их сбор, если в кодовой базе есть секреты в локалах; scrub по ключам не покрывает frame vars.
- **Lesson (scrub-паттерны под РЕАЛЬНЫЕ секреты проекта, узко):** generic `*_token`/`*_secret` пропускает `api_hash`, `nowpayments_api_key`, `postgres_password`, `session`, `*_dsn`. Расширил exact+суффиксы прицельно (`_api_key`/`_api_hash`/`_password`/`_session(s)`/`_dsn`), НО без bare `_key`/`_hash` (скрабили бы невинные `sort_key`/`media_hash` — поймал тест). raw-content (`text`/`content`) дропать во ВСЕХ секциях (extra/contexts/breadcrumbs), не только request.data; non-dict request.data (raw body) → дропать целиком; рекурсия в списки (list[dict]); depth-limit. **How to apply:** scrub-листы выводи из реального реестра секретов проекта (Settings-поля), тестируй и на false-positive (невинные поля не должны скрабиться), и на покрытие (каждый секрет замаскирован).
- **Gotcha (CRITICAL reliability — Celery перехватывает root logger; configure_logging на импорте не выживает):** `configure_logging()` (JSON + RequestIdFilter) вызывался только в api/main.py; worker при старте через `worker_hijack_root_logger` заменяет хендлеры → worker-логи в текстовом формате Celery, без request_id. import-time вызов в celery_app НЕ помогает (Celery хайджекит ПОСЛЕ импорта). **FIX:** receiver на сигнал `setup_logging` (если есть receiver — Celery НЕ конфигурит логирование сам, отдаёт нам) → `configure_logging()`. Ловится только реальным `make up` worker (G2), не unit. **How to apply:** чтобы своё логирование выжило в Celery worker/beat — конфигурь через `setup_logging`-сигнал, не просто на импорте модуля.
- **Decision (сквозной correlation id):** единый `request_id`(uuid4) в `ContextVar`; HTTP-middleware `coerce_request_id` (валидирует входящий `X-Request-ID` через `uuid.UUID` — мусор/инъекция → свой uuid, no header/log-injection) + echo в `X-Request-ID` header; `RequestIdFilter` на лог-хендлере инжектит id в КАЖДУЮ запись (не только log_event); Celery `before_task_publish`→headers / `task_prerun`→contextvar (Beat-цепочка генерит свой) / `task_postrun`→reset (prefork-изоляция). Связывает логи api↔worker одной операции. **How to apply:** correlation-id — через contextvar + logging.Filter (покрывает все логгеры) + проброс в Celery headers; входящий trace-header доверяй только валидным uuid.
- **Gotcha (middleware finally не должен трогать `response` при исключении):** `try: response=await call_next() finally: log(status=response.status_code)` — если call_next бросил, `response` unbound → `UnboundLocalError` маскирует ОРИГИНАЛьное исключение (то, что нужно Sentry) + токен contextvar не сброшен (утечка id). **FIX:** success-path (log/header/return) внутри try; в finally ТОЛЬКО `reset_request_id(token)`. **How to apply:** в middleware с finally сбрасывай только то, что гарантированно установлено (токен); не обращайся к результату call_next в finally.
- **Lesson (release-тег Sentry надо реально прокинуть):** `Settings.release` объявлен и передан в init, но не было `RELEASE` в env-источнике → в проде всегда дефолт "dev". Прокинул `RELEASE={{ app_version }}` в `deploy.env.j2`. **How to apply:** новое Settings-поле, влияющее на прод-поведение, — заведи в env-источник (ansible template+group_vars), иначе тихо останется дефолт.
- **Gotcha (optional secret в sensitive.env.j2 → `| default('')`):** `SENTRY_DSN={{ vault_sentry_dsn }}` без default ломает `make ansible-unpack` на хостах без ключа в vault (boot-секреты обязательны и падать ДОЛЖНЫ, но опциональный DSN — нет). **FIX:** `{{ vault_sentry_dsn | default('') }}` (пустой = Sentry off). **How to apply:** опциональные секреты в j2 — с `| default('')`; обязательные boot-секреты — без (пусть падает).
