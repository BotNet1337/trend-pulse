# Ansible inventory (skeleton — real hosts/playbooks land in task-012).
# Localhost is used by `make ansible-unpack` to render env files locally.

[local]
localhost ansible_connection=local

[prod]
# trendpulse-prod ansible_host=<vps-ip> ansible_user=deploy
